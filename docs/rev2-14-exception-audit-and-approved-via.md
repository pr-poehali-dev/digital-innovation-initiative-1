# Редакция 2. Документ 14.
# Различение причин прекращения исключения. Условия approved_via.
# Уточнённая формулировка независимости

Редакция: 2 · Статус: **Действует** · Дата: 10.08.2026

**Изменения только документальные.** Ограничения, триггеры,
переходы статусов не реализованы.

---

## 1. Подтверждение коммита `b2bfe30`

| Параметр | Значение |
|---|---|
| Полный хеш | `b2bfe3089a2aeafdab5fe8101e77ea2e6b683dff` |
| Сообщение | `[CHORE] Утвердил правила архивирования и проверки` |
| Присутствует в `main` | **Да**, HEAD на момент проверки |
| `git status` | Чисто |

### 1.1. Полный перечень 17 путей

**Документы — 3:**

| № | Путь |
|---|---|
| 15 | `docs/INDEX.md` |
| 16 | `docs/rev2-11-full-test-diff.md` |
| 17 | `docs/rev2-13-lifecycle-and-safeguard.md` |

**Артефакты публикации — 14**, все в
`builds/029cece5ab672d242a5339506a1c4d9c0a3f1263/0c0e8/`.

### 1.2. Статистика

| Показатель | Значение |
|---|---|
| Файлов изменено | 17 |
| Добавлено строк | 8970 |
| Удалено строк | 43 |
| Путей вне `docs/` и `builds/` | **0** |
| Изменений в `src/`, `backend/`, `db_migrations/`, `package.json` | **Нет** |

**Классификация:** документальные изменения плюс генерируемые
артефакты сборки — без изменений продуктового кода, схемы,
данных, миграций, авторизации и production-конфигурации.

---

## 2. Уточнённая формулировка независимости — принята дословно

> **Правило независимого подтверждения действует без ролевых
> исключений. Аварийное принятие собственной записи по
> исключению допускается отдельно и не является независимым
> подтверждением.**

Формулировка зафиксирована как основная в терминологии проекта.
`accepted_by_exception` для собственной записи **не считается
исключением из правила независимости**, поскольку само по себе
**не является подтверждением** — это отдельная, явно иначе
поименованная операция.

---

## 3. Различение причин прекращения исключения

**Принято требование:** истечение календарного срока и досрочное
прекращение из-за содержательного изменения должны быть
различимы в аудите, хотя оба лишают исключение действующей силы.

### 3.1. Единый признак утраты силы, различимая причина

| Поле | Назначение |
|---|---|
| `exception_active` | Вычисляемое: `false`, если исключение утратило силу по любой причине |
| `exception_termination_reason` | **Причина**, обязательна при `exception_active = false` |

### 3.2. Допустимые значения `exception_termination_reason`

| Код | Причина | Что фиксируется дополнительно |
|---|---|---|
| `expired` | Истёк календарный срок (`exception_expires_at` в прошлом, UTC) | Дата истечения — совпадает с `exception_expires_at` |
| `superseded_by_change` | Досрочно прекращено содержательным изменением записи | Дата и автор изменения, которое прекратило исключение |
| `null` | Исключение продолжает действовать | — |

**Оба случая лишают исключение силы одинаково** с точки зрения
допустимости операций (`X03` из `rev2-13`), но причина в аудите
**не смешивается**.

### 3.3. Поведение по каждой причине

| Аспект | `expired` | `superseded_by_change` |
|---|---|---|
| Момент фиксации | В момент проверки признака `exception_expired`, дата истечения известна заранее | В момент содержательного изменения — событие, а не таймер |
| Что становится недействительным | Только исключение | Исключение **и** предыдущее содержание записи |
| Требуется ли новое решение | Да — новое исключение или независимая проверка | Да — новое исключение или независимая проверка **для изменённого содержания** |
| История прежнего исключения | Сохраняется целиком | Сохраняется целиком |

### 3.4. Отображение в аудите

```
Запись: role_assignment #4821
├─ 10.08.2026 09:00  accepted_by_exception
│                     основание: «отсутствие второго редактора»
│                     срок: 17.08.2026 23:59 UTC
│                     автор: управляющий кабинетом
│
└─ 15.08.2026 14:22  exception_active → false
                      причина: superseded_by_change
                      прекращено изменением: смена субъекта роли
                      автор изменения: редактор Х
```

```
Запись: decision_instance #190
├─ 10.08.2026 09:00  accepted_by_exception
│                     срок: 12.08.2026 23:59 UTC
│
└─ 13.08.2026 00:00  exception_active → false
                      причина: expired
                      (календарный срок наступил, изменений
                       содержания не было)
```

**Обе ситуации визуально и структурно различимы.**

---

## 4. Условия `approved_via` — полный набор

### 4.1. Ограничение на использование `accepted_by_exception`

**Принято дословно:** значение `approved_via = accepted_by_exception`
допустимо **только при наличии отдельного решения**, разрешающего
утверждение без независимого подтверждения. Сам факт нахождения
записи в статусе `accepted_by_exception` **автоматически
`approved` не порождает**.

### 4.2. Обязательные поля решения об утверждении через исключение

| Поле | Назначение |
|---|---|
| `approval_decision_ref` | Ссылка или идентификатор **отдельного** решения, разрешающего утверждение без независимого подтверждения |
| `approval_decision_author` | Автор решения об утверждении |
| `approval_decision_date` | Дата решения |
| `approval_decision_basis` | Основание |
| `approval_based_on_exception_ref` | Ссылка на **действовавшее** исключение (`accepted_by_exception`), на котором основано утверждение |
| `approval_exception_expires_at` | Срок этого исключения **на момент утверждения** — сохраняется как снимок, не пересчитывается впоследствии |
| `retrospective_review_result` | Результат последующей ретроспективной проверки — заполняется отдельно, когда проверка состоится |

**Все семь полей обязательны** при `approved_via =
accepted_by_exception`. При `approved_via = confirmed`
поля, специфичные для исключения, не заполняются.

### 4.3. Прозрачность просрочки и досрочного прекращения после `approved`

**Принято:** переход в `approved` не должен скрывать просрочку
или досрочное прекращение исключения, на котором он основан.

| Ситуация | Отображение |
|---|---|
| `approved` установлен, исключение позже истекло | `approved` **сохраняется** формально; на карточке записи отображается предупреждение: «Утверждено на основании исключения, впоследствии истёкшего {дата}» |
| `approved` установлен, исключение позже прекращено содержательным изменением **того же исключения до утверждения** | Не может произойти — `approval_based_on_exception_ref` фиксируется на момент утверждения снимком |
| Основание для `approved` утрачивает силу **после** утверждения | Реакция уже утверждённой записи — **отдельный вопрос реализации**, не специфицируется в этом документе |

### 4.4. Явное разграничение вопросов

**Зафиксировано как открытое, по вашему указанию:** конкретная
реакция уже утверждённой записи на последующую просрочку
лежавшего в её основе исключения **не решается сейчас** —
отдельный вопрос реализации следующего этапа.

**Решается сейчас:** сам факт прохождения через исключение
**не скрывается** — виден в `approved_via` и в семи
сопроводительных полях бессрочно, независимо от дальнейшей
судьбы исключения.

---

## 5. Полный тестовый diff — уточнение статуса

**Подтверждаю:** unified diff пяти файлов был приведён полностью
в `rev2-11-full-test-diff.md`, раздел 4, при подготовке этого
документа. Содержание не менялось в части архитектуры, кроме
исправленного предохранителя (раздел 7 этого документа, п. 6).

**Статус остаётся в точности как вы зафиксировали:**

| Пункт | Статус |
|---|---|
| Документ-проект | Подготовлен |
| Тестовые файлы | **Не созданы** |
| Diff | **Не применён** |
| Архитектура предохранителя | Пересмотрена по замечанию — этап 1 дополнен четырьмя проверками, этап 2 дополнен вторым независимым признаком |
| Содержимое пяти файлов | **Не утверждено** |
| Запуск тестов | **Запрещён** |

---

## 6. Исправление предохранителя — сводка изменений

По вашему замечанию `rev2-11` дополнен:

| Было | Стало |
|---|---|
| Этап 1: 2 проверки (`TEST_ENV_CONFIRMED`, `TEST_PROJECT_ID`) | Этап 1: **4 проверки** — те же две **плюс** локальный маркер тестового пакета в коде **плюс** проверка полноты всех 5 переменных **плюс** явный флаг запрета HTTP/записи до прохождения |
| Этап 2: 1 признак (`current_database()` содержит `test`) | Этап 2: **2 независимых признака** — имя базы **плюс** отдельная таблица-маркер `test_environment_marker.purpose = 'isolated-audit-mode-test'`, читаемая только диагностически в режиме read-only |

**Требование к среде дополнено пунктом 11** в `rev2-12` —
создание таблицы-маркера как часть подготовки изолированного
контура.

---

## 7. Что НЕ выполнялось

| Действие | Статус |
|---|---|
| Реализация признаков `exception_active`, `exception_termination_reason` | **Не выполнялась** — концептуальная фиксация |
| Реализация полей `approval_*` | **Не выполнялась** |
| Изменение продуктового кода, схемы, миграций, данных | **Не выполнялось** |
| Создание тестовых файлов | **Не выполнялось** |
| Подача заявки на среду | **Не выполнялась** — требует действия владельца проекта |
| Запуск тестов | **Не выполнялся** |

---

## 8. Открытые вопросы

| № | Вопрос |
|---|---|
| N01 | Утверждаете различение `expired` / `superseded_by_change` как обязательное для аудита? |
| N02 | Утверждаете семь обязательных полей для `approved_via = accepted_by_exception`? |
| N03 | Переносить ли вопрос реакции `approved` на последующую просрочку основания в план следующего этапа реализации явным пунктом? |
| N04 | Подтверждаете дополненную архитектуру предохранителя (4 проверки этапа 1, 2 признака этапа 2) как окончательную для будущего рассмотрения на применение? |
