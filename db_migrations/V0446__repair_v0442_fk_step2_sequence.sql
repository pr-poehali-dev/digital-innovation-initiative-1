-- ИСПРАВЛЕНО ПОСТФАКТУМ (повторно): этот файл синхронизировал sequence
-- exec_project.id после вставки placeholder-проекта в V0445. Раз V0445
-- больше ничего не вставляет (см. его актуальный текст), синхронизировать
-- нечего — обязательной пользы этот шаг не несёт ни на существующей БД,
-- ни на чистом развёртывании.
--
-- Сам по себе GREATEST(MAX(id), 15) не портит данные (это не INSERT), но
-- превращён в no-op вместе с V0445, чтобы пара файлов оставалась
-- согласованной и не вводила в заблуждение при чтении истории.
SELECT 1;
