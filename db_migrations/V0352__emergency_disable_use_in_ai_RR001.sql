-- ЭКСТРЕННОЕ ОГРАНИЧЕНИЕ по решению владельца (RR-001), выполнено 2026-09-11.
-- Основание: "Экстренное ограничение использования материалов в AI-контексте до определения
-- их конфиденциальности, допустимости обработки в YandexGPT и принятия решения по RR-001".
-- Операция функционально идентична вызову штатного action=toggle_ai для каждой из 6 записей
-- (тот же SQL, что выполняет backend/exec-knowledge/index.py при toggle_ai).
-- Выполнено напрямую, т.к. у агента нет пользовательской HTTP-сессии/admin-токена
-- для вызова API как реального актора.
-- Тексты, чанки, статусы и заголовки записей НЕ изменяются. Удаление не выполняется.

UPDATE exec_knowledge
SET use_in_ai = false,
    updated_at = now()
WHERE id IN (1,2,3,4,5,6);
